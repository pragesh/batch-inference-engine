/**
 * Application configuration types for the batch inference engine.
 * <ul>
 *   <li>{@link com.batchinference.config.AppProperties} — worker pool, chunking, retry tuning, data paths</li>
 *   <li>{@link com.batchinference.config.InferenceProperties} — LLM provider URL, API key, model selection</li>
 *   <li>{@link com.batchinference.config.SpacesProperties} — DigitalOcean Spaces checkpoint streaming</li>
 * </ul>
 */
package com.batchinference.config;
