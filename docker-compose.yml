services:
  batch-inference:
    build: .
    ports:
      - "8080:8080"
    environment:
      INFERENCE_PROVIDER: mock
      WORKER_POOL_SIZE: 10
      CHUNK_SIZE: 50
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
